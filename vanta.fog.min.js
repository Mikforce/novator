\n}\n\nvoid main() {\n vec2 st = gl_FragCoord.xy / iResolution.xy*3.;\n st.x *= 0,7 * iResolution.x / iResolution.y ; // Тем не менее оставить его более горизонтальным, чем квадратным\n st *= zoom;\n\n // st += st * abs(sin(iTime*0.1)*3.0);\n vec3 color = vec3(0.0);\ n\n vec2 q = vec2(0.);\n qx = fbm( st + 0.00*iTime);\n qy = fbm( st + vec2(1.0));\n\n vec2 dir = vec2(0.15, 0.126);\n vec2 r = vec2(0.);\n rx = fbm( st + 1.0*q + vec2(1.7,9.2)+ dir.x*iTime );\n ry = fbm( st + 1.0* q + vec2(8.3,2.8)+ dir.y*iTime);\n\n float f = fbm(st+r);\n\n color = mix(baseColor,\n lowlightColor,\n clip((f *f)*4.0,0.0,1.0));\n\n цвет = смесь(цвет,\nсредний цвет,\n зажим(длина(q),0.0,1.0));\n\n цвет = смесь(цвет ,\nhighlightColor,\nclamp(length(rx),0.0,1.0));